@model IEnumerable<SchoolManagement.Web.Models.SubjectViewModel>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Subjects";
}

<div class="container-fluid">
    <h1 class="mt-4">Admin Dashboard</h1>
    <p>Welcome to the management panel.</p>

    <div class="row mt-4">
        <div class="col-md-12">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h5 class="card-title">Subjects</h5>

                    <ejs-button id="btnAddSubject"
                                cssClass="e-success mb-3"
                                content="Add New Subject"
                                iconCss="fas fa-book-medical"
                                iconPosition="Left"
                                onclick="location.href='/AdminDashboard/Subjects/Create'">
                    </ejs-button>

                    <div id="SubjectsGrid"></div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" style="max-width: 400px;">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="deleteModalLabel">Confirm Delete</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete the subject:</p>
                <h6 id="subjectName"></h6>
            </div>
            <div class="modal-footer">
                <form id="deleteForm" method="post">
                    <input type="hidden" name="id" id="subjectIdToDelete" />

                    <ejs-button id="confirmDeleteBtn"
                                type="submit"
                                content="Delete"
                                iconCss="fas fa-trash"
                                cssClass="e-danger">
                    </ejs-button>

                    <ejs-button id="cancelDeleteBtn"
                                content="Cancel"
                                iconCss="fas fa-times"
                                cssClass="e-secondary"
                                onclick="bootstrap.Modal.getInstance(document.getElementById('deleteModal')).hide(); return false;">
                    </ejs-button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        var subjects = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model));

        var grid = new ej.grids.Grid({
            dataSource: subjects,
            allowPaging: true,
            allowSorting: true,
            allowFiltering: true,
            filterSettings: { type: 'CheckBox' },
            pageSettings: { pageSize: 10 },
            columns: [
                { field: 'Name', headerText: 'Name', width: 200 },
                { field: 'Workload', headerText: 'Workload (hours)', width: 150, textAlign: 'Right' },
                { field: 'AllowedAbsences', headerText: 'Allowed Absences', width: 150, textAlign: 'Right' },
                {
                    headerText: 'Actions',
                    width: 300,
                    template: function (data) {
                        return `
                            <button class="e-btn e-warning me-1" onclick="location.href='/AdminDashboard/Subjects/Edit/${data.Id}'">
                                <i class="fas fa-pen"></i> Edit
                            </button>
                            <button class="e-btn e-primary me-1" onclick="location.href='/AdminDashboard/Subjects/Details/${data.Id}'">
                                <i class="fas fa-eye"></i> Details
                            </button>
                            <button class="e-btn e-danger" onclick="showDeleteModal('${data.Id}', '${data.Name}')">
                                <i class="fas fa-trash"></i> Delete
                            </button>
                        `;
                    },
                    textAlign: 'Center'
                }
            ]
        });

        grid.appendTo('#SubjectsGrid');

        function showDeleteModal(id, name) {
            document.getElementById('subjectName').innerText = name;
            document.getElementById('subjectIdToDelete').value = id;
            document.getElementById('deleteForm').action = `/AdminDashboard/Subjects/Delete/${id}`;
            var modal = new bootstrap.Modal(document.getElementById('deleteModal'));
            modal.show();
        }
    </script>
}
